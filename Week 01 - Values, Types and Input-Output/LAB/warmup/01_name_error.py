# BROKEN ON PURPOSE.
# Run it, read the last line, then fix it.

record_id = "R-004"

print(record_ID)

# Error: record_ID is not defined. 
# Cause: Python is case-sensitive, so record_id and 
# record_ID are two different variables. 

# Fixed code:
record_id = "R-004"
print(record_id)
